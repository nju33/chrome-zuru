{
  "name": "bg",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "parcel": "^1.9.2",
    "ws": "^5.2.0"
  },
  "scripts": {
    "dev": "parcel watch background.html --public-url ./",
    "prebuild": "rm -rf .cache dist",
    "build": "parcel build background.html --public-url ./"
  }
}
