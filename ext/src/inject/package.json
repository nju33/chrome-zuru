{
  "name": "inject",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "parcel": "^1.9.2"
  },
  "scripts": {
    "dev": "parcel watch inject.js",
    "prebuild": "rm -rf .cache dist",
    "build": "parcel build inject.js"
  }
}
